name = "english-roman-translator-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# Environment variables (set via Cloudflare dashboard or wrangler secret)
# Don't put actual secrets here!
[vars]
# GROQ_API_KEY will be set as a secret

# For local development
[env.dev]
# Use wrangler secret put GROQ_API_KEY to set locally